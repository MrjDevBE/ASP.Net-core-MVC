@model IEnumerable<TestUMS.Models.Airport>



    @{
    ViewData["Title"] = "Airports";
    }

    <h2>Airports</h2>

    <p>
        <a asp-action="Create">Add</a>
    </p>

    <table class="table">
        <thead>
            <tr>
                <th>Actions</th>
                <th>Airport Name</th>
                <th>Address</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
            <tr>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                    <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
                </td>
                <td>@item.Name</td>
                <td>@item.Address</td>
            </tr>
            }
        </tbody>
    </table>
